# Middleware Dependencies - SignalR Only (Optimized)
signalrcore==0.9.5
pydantic==2.5.0
structlog==23.2.0
pyyaml==6.0.1

# Performance optimization dependencies
aiofiles==23.2.1
psutil==5.9.6
uvloop==0.19.0; sys_platform != "win32"

# Optional: For better JSON performance
orjson==3.9.10

# Optional: For connection pooling
aioredis==2.0.1
