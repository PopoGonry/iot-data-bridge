# IoT Data Bridge Main Configuration

app_name: "IoT Data Bridge"
mapping_catalog_path: "config/mappings.yaml"
device_catalog_path: "config/devices.yaml"

# Input Layer Configuration
input:
  type: "mqtt"  # or "signalr"
  mqtt:
    host: "localhost"          # 로컬 개발: localhost, 멀티 VM: 192.168.1.100
    port: 1883
    username: null
    password: null
    topic: "iot/ingress"
    qos: 1
    keepalive: 60
    ssl: false
  signalr:
    url: "http://localhost:5000/hub"  # 로컬 개발: localhost, 멀티 VM: 192.168.1.100
    group: "iot_clients"
    username: null
    password: null

# Transports Layer Configuration
transports:
  type: "mqtt"  # or "signalr"
  mqtt:
    host: "localhost"          # 로컬 개발: localhost, 멀티 VM: 192.168.1.100
    port: 1883
    username: null
    password: null
    topic: "devices/{device_id}/ingress"
    qos: 1
    keepalive: 60
    ssl: false
  signalr:
    url: "http://localhost:5000/hub"  # 로컬 개발: localhost, 멀티 VM: 192.168.1.100
    group: "devices"
    username: null
    password: null

# Logging Configuration
logging:
  level: "INFO"
  file: "logs/iot_data_bridge.log"
  max_size: 10485760  # 10MB
  backup_count: 5
